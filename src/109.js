// Example of generating random code to illustrate complex logic or algorithms
function createRandomCode() {
    // This is just an example. You can replace it with your own logic.
    const randomNumber = Math.floor(Math.random() * 100) + 5; // Generates a number between 5 and 100
    return `Here's a piece of code to demonstrate complex logic:
    \`\`\`
    let i = 0;
    while (i < 100) {
        console.log(`Random value: ${randomNumber}`);
        i++;
    }
    `;
}

console.log(createRandomCode());

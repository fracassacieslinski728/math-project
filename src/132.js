// Example of an unoptimized JavaScript code

function sumNumbers(a, b) {
    return a + b;
}

console.log(sumNumbers(2, 3)); // Output: 5

function multiplyNumbers(num1, num2) {
    return num1 * num2;
}

console.log(multiplyNumbers(4, 6)); // Output: 24

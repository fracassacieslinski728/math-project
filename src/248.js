// Example of a simple JavaScript function to multiply two numbers
function multiply(a, b) {
  return a * b;
}

// Example of a simple JavaScript function to add two numbers
function add(a, b) {
  return a + b;
}

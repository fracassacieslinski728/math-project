// Example: A simple function to calculate the area of a rectangle

function calculateArea(length, width) {
  return length * width;
}

// Another example with different parameters
function calculateVolume(height, baseArea) {
  let volume = height * baseArea;
  console.log(`The volume of the rectangular prism is ${volume.toFixed(2)}`);
}

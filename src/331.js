// This is an example of a simple JavaScript function to sum two numbers.
function addNumbers(a, b) {
    return a + b;
}

// Example usage:
const result = addNumbers(5, 3);
console.log(result); // Output: 8

// This is an example of a basic function to calculate the area of a rectangle.
function calculateArea(width, height) {
    return width * height;
}

// Example usage:
var width = 10;
var height = 5;
console.log(calculateArea(width, height)); // Output: 50

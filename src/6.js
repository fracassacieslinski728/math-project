// Example code for a simple math function
function add(x, y) {
  return x + y;
}

// Example code for a simple math equation
const result = add(2, 3);
console.log(result); // Output: 5

// Example code for a more complex math operation
function subtract(x, y) {
  return x - y;
}

const result = subtract(4, 2);
console.log(result); // Output: 2

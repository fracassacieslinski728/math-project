// Example JavaScript code for generating a random number between 1 and 10

function getRandomNumber() {
    const random = Math.floor(Math.random() * 10);
    return random;
}

console.log(getRandomNumber());
